Legacy production compose assets were archived here when the repo moved to the single maintained deployment path:

`Terraform -> Ansible bootstrap -> single-node k3s -> HTTPS ingress`

These files are historical lab evidence only. They are not the maintained VM deployment flow anymore.
